@model ITGlobalProject.Models.Employees

@{
    ViewBag.Title = "Thông Tin Nhân Viên";
    Layout = "~/Areas/Admins/Views/Shared/_AdminLayout.cshtml";
    string imagesUrl = "";
    if (string.IsNullOrEmpty(Model.Avatar))
    {
        imagesUrl = "~/Content/Admin/assets/images/avatar/default-avatar.png";
    }
    else
    {
        imagesUrl = Model.Avatar;
    }
}
<input hidden value="@Model.ID" id="idus" />
<input hidden value="@Model.ID_Employee" id="maNhanVienss" />

<input hidden value="@Url.Content("~/ValidationAlert/Admin/QuanLyNhanSu/KhoaTaiKhoanValidation.js")" id="reloadScriptLock" />
<input hidden value="@Url.Content("~/Content/Admin/assets/images/avatar/default-avatar.png")" id="urlIgmStr" />
<div id="actionSubmit" hidden data-request-url="@Url.Action("chinhSuaThongTinPartial", "QuanLyNhanSu")"></div>
<div id="actionKhoaTaiKhoan" hidden data-request-url="@Url.Action("khoaTaiKhoan", "QuanLyNhanSu")"></div>
<div id="actionDanhsach" hidden data-request-url="@Url.Action("danhSachNhanVien", "QuanLyNhanSu")"></div>
<div id="contentPartial" class="container-fluid p-4">
    <div class="row">
        <!-- Page Header -->
        <div class="col-lg-12 col-md-12 col-12">
            <div class="border-bottom pb-4 mb-4 d-flex justify-content-between align-items-center">
                <div class="mb-2 mb-lg-0">
                    <h1 class="mb-1 h2 fw-bold">
                        Thông Tin Chi Tiết
                    </h1>
                    <!-- Breadcrumb  -->
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item">
                                <a href="@Url.Action("Overview", "Dashboard")">Trang Chủ</a>
                            </li>
                            <li class="breadcrumb-item"><a href="@Url.Action("danhSachNhanVien", "QuanLyNhanSu")">Quản Lý Nhân Sự</a></li>
                            <li class="breadcrumb-item"><a href="@Url.Action("danhSachNhanVien", "QuanLyNhanSu")">Nhân Viên</a></li>

                            <li class="breadcrumb-item active" aria-current="page">
                                Thông Tin Chi Tiết
                            </li>
                        </ol>
                    </nav>
                </div>
                <div>
                    <a id="taiXuongNhanVien" class="btn btn-primary me-2 text-nowrap"><i class="fe fe-download"></i>&ensp;Tải Xuống Thông Tin Nhân Viên</a>
                </div>
            </div>
        </div>
    </div>

    <!-- User info -->
    <div class="row">
        <div class="col-xl-12 col-lg-12 col-md-12 col-12">
            <!-- Bg -->
            <div class="pt-16 rounded-top-md" style="
								background: url(@Url.Content("~/Content/Admin/assets/images/background/profile-bg.jpg")) no-repeat;
								background-size: cover;
							"></div>
            <div class="d-flex align-items-end justify-content-between bg-white px-4 pt-2 pb-4 rounde
                     rounded-bottom-md shadow-sm">
                <div class="d-flex align-items-center">
                    <div class="me-2 position-relative d-flex justify-content-end align-items-end mt-n5">
                        <img src="@Url.Content(imagesUrl)" class="avatar-xl rounded-circle border border-4 border-white"
                             alt="" />
                    </div>
                    <div class="lh-1">
                        <h2 class="mb-0">
                            @Model.Name
                            @if (Model.Lock == false)
                            {
                                <i id="CheckorX" class="mdi mdi-check-circle-outline text-success" data-bs-toggle="tooltip" data-placement="top" title="Hoạt động"></i>
                            }
                            else
                            {
                                <i id="CheckorX" style="font-size: 22px" class="fe fe-x-circle text-danger" data-bs-toggle="tooltip" data-placement="top" title="Đã ngưng làm"></i>
                            }
                        </h2>
                        <p class="mb-0 d-block">@Model.WorkEmail</p>
                    </div>

                </div>
                <div>
                    @if (Model.Lock == true)
                    {
                        <span id="ngaythoiviec" typeof="text" style="float: right; margin-top:1%; padding-left: 10px" class="btn btn-outline-danger btn-sm">Ngày thôi việc: @Convert.ToDateTime(Model.DayOff).ToString("yyyy-MM-dd")</span><span id="ngaythoiviecspace" style="width: 20px; float: right;">&ensp;</span>
                        <span typeof="text" style="float: right; margin-top:1%" class="btn btn-outline-dark btn-sm">Ngày vào làm: @Model.JoinedDate.ToString("yyyy-MM-dd")</span>
                    }
                    else
                    {
                        <span hidden="hidden" id="ngaythoiviec" typeof="text" style="float: right; margin-top:1%; padding-left: 10px" class="btn btn-outline-danger btn-sm">Ngày thôi việc: @Convert.ToDateTime(Model.DayOff).ToString("yyyy-MM-dd")</span><span hidden="hidden" id="ngaythoiviecspace" style="width: 20px; float: right;">&ensp;</span>
                        <span typeof="text" style="float: right; margin-top:1%" class="btn btn-outline-dark btn-sm">Ngày vào làm: @Model.JoinedDate.ToString("yyyy-MM-dd")</span>
                    }
                </div>

            </div>
        </div>
    </div>
    <!-- Content -->
    <div class="row mt-0 mt-md-4">
        <div class="col-lg-3 col-md-4 col-12">
            <!-- Side navbar -->
            <nav class="navbar navbar-expand-md navbar-light shadow-sm mb-4 mb-lg-0 sidenav">
                <!-- Menu -->
                <a class="d-xl-none d-lg-none d-md-none text-inherit fw-bold" href="#">Menu</a>
                <!-- Button -->
                <button class="navbar-toggler d-md-none icon-shape icon-sm rounded bg-primary text-light" type="button"
                        data-bs-toggle="collapse" data-bs-target="#sidenav" aria-controls="sidenav" aria-expanded="false"
                        aria-label="Toggle navigation">
                    <span class="fe fe-menu"></span>
                </button>
                <!-- Collapse navbar -->
                <div class="collapse navbar-collapse" id="sidenav">
                    <div class="navbar-nav flex-column">
                        <span class="navbar-header">Hồ Sơ Nhân Viên</span>
                        <!-- List -->
                        @*<ul class="list-unstyled ms-n2 mb-4">*@
                        <ul class="list-unstyled ms-n2 mb-4">
                            <!-- Nav item -->
                            <li class="nav-item active">
                                <a role="button" id="ttcanhan" class="nav-link">
                                    <i class="fe fe-user nav-icon"></i>Thông tin cá nhân
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" id="knchuyenmon" class="nav-link">
                                    <i class="fe fe-code nav-icon"></i>Kỹ năng chuyên môn
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" id="tdngoaingu" class="nav-link">
                                    <i class="fe fe-award nav-icon"></i>Trình độ ngoại ngữ
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" id="ptnhanthan" class="nav-link">
                                    <i class="fe fe-users nav-icon"></i>Người phụ thuộc
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" id="hdvacongviec" class="nav-link">
                                    <i class="fe fe-file-text nav-icon"></i>Hợp đồng & Công việc
                                </a>
                            </li>
                        </ul>

                        <span class="navbar-header">Thông Tin Lương</span>
                        <ul class="list-unstyled ms-n2 mb-4">
                            <!-- Nav item -->
                            <li class="nav-item ">
                                <a role="button" id="lhvathanhtoan" class="nav-link">
                                    <i class="fe fe-phone-call nav-icon"></i>Tiền Lương & Thanh toán
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" id="tcvaphucap" class="nav-link">
                                    <i class="fe fe-trending-up nav-icon"></i>Trợ cấp & Phụ cấp
                                </a>
                            </li>
                            <li class="nav-item">
                                <a role="button" id="bangLuongs" class="nav-link">
                                    <i class="fe fe-dollar-sign nav-icon"></i>Bảng lương
                                </a>
                            </li>
                        </ul>

                        <span class="navbar-header">quản lý nghỉ phép</span>
                        <ul class="list-unstyled ms-n2 mb-4">
                            <!-- Nav item -->
                            <li class="nav-item ">
                                <a role="button" id="donnghiphep" class="nav-link">
                                    <i class="fe fe-clock nav-icon"></i>Đơn nghỉ phép
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" id="danhmucngayphep" class="nav-link">
                                    <i class="fe fe-calendar nav-icon"></i>Danh Mục Ngày Phép
                                </a>
                            </li>
                        </ul>

                        <span class="navbar-header">Dự án & Công việc</span>
                        <ul class="list-unstyled ms-n2 mb-0">
                            <!-- Nav item -->

                            <li class="nav-item">
                                <a role="button" class="nav-link" id="DAThamGia">
                                    <i class="fe fe-bookmark nav-icon"></i>Dự án tham gia
                                </a>
                            </li>
                            <li class="nav-item  ">
                                <a role="button" class="nav-link" id="LBCongViec">
                                    <i class="fe fe-calendar nav-icon"></i>Tiến độ công việc
                                </a>
                            </li>
                            <li class="nav-item ">
                                <a role="button" class="nav-link" id="LSHoatDong">
                                    <i class="fe fe-clock nav-icon"></i>Lịch sử hoạt động
                                </a>
                            </li>
                            @*<li class="nav-item  ">
                                <a role="button" class="nav-link" id="BCThongKe">
                                    <i class="fe fe-pie-chart nav-icon"></i>Báo cáo thống kê
                                </a>
                            </li>*@
                            <hr />
                            @if (Model.Lock == false)
                            {
                                <li style="text-align:center; margin-top:20px">
                                    <a id="khoataikhoan" style="width:100%" class="btn btn-outline-danger btn-sm">
                                        KHÓA TÀI KHOẢN <i class="mdi mdi-lock "></i>
                                    </a>
                                </li>
                            }
                            else
                            {
                                <li style="text-align:center; margin-top:20px">
                                    <a id="khoataikhoan" style="width:100%" class="btn btn-outline-success btn-sm">
                                        MỞ KHÓA TÀI KHOẢN <i class="mdi mdi-lock "></i>
                                    </a>
                                </li>
                            }
                        </ul>

                    </div>
                </div>
            </nav>
        </div>
        <div class="col-lg-9 col-md-8 col-12">
            <!-- Card -->
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0">Thông Tin Cá Nhân  </h3>
                </div>
                <div class="card-body">
                    <div id="reloadImage" class="d-lg-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center mb-4 mb-lg-0">
                            <img src="@Url.Content(imagesUrl)" class="avatar-xl rounded-circle border border-4 border-white"
                                 alt="" id="previewImage" />
                            <div class="ms-3">
                                <h4 class="mb-0">Hình đại diện</h4>
                                <p class="mb-0">
                                    Tải lên ảnh đại diện với định dạng hình ảnh.
                                </p>
                            </div>
                        </div>
                        <div>
                            <input hidden id="selectFiles" type="file" name="file" accept="image/*" onchange="ShowImageUpload(this, document.getElementById('previewImage'))" />
                            <a role="button" id="clickFiles" class="btn btn-outline-primary btn-sm">Tải lên</a>
                            <a role="button" id="reloadButton" class="btn btn-outline-danger btn-sm">Xóa bỏ</a>
                        </div>
                    </div>
                    <hr class="my-4" />
                    <div>
                        <form id="noidungPage" class="row">
                            <div class="mb-2 col-12 col-md-12">
                                <label style="font-weight:bold;" class="form-label" for="fname">Họ và Tên <span class="text-danger">*</span></label>
                                <input value="@Model.Avatar" name="avatar" id="avatar" hidden />
                                <input value="@Model.Name" type="text" id="hoten" name="hoten" class="form-control" placeholder="Nguyễn Văn A" />
                                <p style="font-size: 13px; color:red;" id="hotenvalidation"></p>
                            </div>
                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="fname">Số CMND/CCCD <span class="text-danger">*</span></label>
                                <input value="@Model.IdentityCard" type="text" class="form-control" id="cmnd" name="cmnd" data-inputmask="'mask': '9999 9999 9999'" inputmode="numeric"
                                       placeholder="Nhập số chứng minh hoặc căn cước" />
                                <p style="font-size: 13px; color:red;" id="cmndvalidation"></p>
                            </div>
                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="quoctich">Quốc tịch <span class="text-danger">*</span></label>
                                <input value="@Model.Nationality" type="text" id="quoctich" name="quoctich" class="form-control" placeholder="Việt Nam" />
                                <p style="font-size: 13px; color:red;" id="quoctichvalidation"></p>
                            </div>
                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" l class="form-label" for="birth"> Ngày sinh <span class="text-danger">*</span></label>
                                <input value="@Model.Birthday.ToString("yyyy-MM-dd")" type="text" class="form-control flatpickr" placeholder="Chọn ngày sinh" id="ngaysinh" name="ngaysinh" />
                                <p style="font-size: 13px; color:red;" id="ngaysinhvalidation"></p>
                            </div>

                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="sodienthoaididong">Số điện thoại di động <span class="text-danger">*</span></label>
                                <input value="@Model.TelephoneMobile" type="tel" id="sodienthoaididong" name="sodienthoaididong" class="form-control" data-inputmask="'mask': '9999 999 999'" inputmode="numeric" placeholder="Nhập số điện thoại di động" />
                                <p style="font-size: 13px; color:red;" id="sodienthoaididongvalidation"></p>
                            </div>
                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="sodienthoaikhac">Số điện thoại nhà</label>
                                <input value="@Model.TelephoneOrther" type="tel" id="sodienthoaikhac" name="sodienthoaikhac" class="form-control" data-inputmask="'mask': '9999 999 999'" inputmode="numeric" placeholder="Nhập số điện thoại nhà" />
                                <p style="font-size: 13px; color:red;" id="sodienthoaikhacvalidation"></p>
                            </div>
                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label">Giới tính <span class="text-danger">*</span></label>
                                <select name="gioitinh" id="gioitinh" class="form-select text-dark" data-width="100%">
                                    @if (Model.Sex.Equals("Nam"))
                                    {
                                        <option selected value="Nam">Nam</option>
                                        <option value="Nữ">Nữ</option>
                                    }
                                    else
                                    {
                                        <option value="Nam">Nam</option>
                                        <option selected value="Nữ">Nữ</option>
                                    }
                                </select>
                                <p style="font-size: 13px; color:red;" id="gioitinhvalidation"></p>
                            </div>

                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="diachiemailcongty">Email Công Ty <span class="text-danger">*</span></label>
                                <input value="@Model.WorkEmail" type="text" id="diachiemailcongty" name="diachiemailcongty" class="form-control" data-inputmask="'alias': 'email'" inputmode="numeric" placeholder="example@itglobal.net" />
                                <p style="font-size: 13px; color:red;" id="diachiemailcongtyvalidation"></p>
                            </div>
                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="diachiemailkhac">Email Khác</label>
                                <input value="@Model.OrtherEmail" type="text" id="diachiemailkhac" name="diachiemailkhac" class="form-control" data-inputmask="'alias': 'email'" inputmode="numeric" placeholder="example@itglobal.net" />
                                <p style="font-size: 13px; color:red;" id="diachiemailkhacvalidation"></p>
                            </div>

                            <div class="mb-2 col-12 col-md-4">
                                <label style="font-weight:bold;" class="form-label" for="honnhan">Tình trạng hôn nhân <span class="text-danger">*</span></label>
                                <select data-width="100%" class="form-select text-dark" id="honnhan" name="honnhan">

                                    @if (Model.MaritalStatus.Equals("Độc thân"))
                                    {
                                        <option selected value="Độc thân">Độc thân</option>
                                        <option value="Đã kết hôn">Đã kết hôn</option>
                                        <option value="Khác">Khác</option>
                                    }
                                    else if (Model.MaritalStatus.Equals("Đã kết hôn"))
                                    {
                                        <option value="Độc thân">Độc thân</option>
                                        <option selected value="Đã kết hôn">Đã kết hôn</option>
                                        <option value="Khác">Khác</option>
                                    }
                                    else
                                    {
                                        <option value="Độc thân">Độc thân</option>
                                        <option value="Đã kết hôn">Đã kết hôn</option>
                                        <option selected value="Khác">Khác</option>
                                    }
                                </select>
                                <p style="font-size: 13px; color:red;" id="honnhanvalidation"></p>
                            </div>

                            <div class="mb-2 col-12 col-md-12">
                                <label style="font-weight:bold;" class="form-label" for="address">Địa chỉ nhà</label>
                                <textarea rows="4" id="diachinha" name="diachinha" class="form-control" placeholder="Nhập vào địa chỉ nhà">@Model.Address</textarea>
                                <p style="font-size: 13px; color:red;" id="diachinhavalidation"></p>
                            </div>
                            <div style="text-align: right; padding-top: 20px" class="mb-3 col-12 col-md-6">
                                <button id="resetData" type="reset" style="width: 230px" class="btn btn-outline-danger">
                                    Hủy
                                </button>
                            </div>
                            <div style="padding-top: 20px" class="mb-3 col-12 col-md-6">
                                <a role="button" id="btnLuuThongTin" style="width: 230px" class="btn btn-success">
                                    Lưu
                                </a>
                            </div>
                        </form>

                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="~/ValidationAlert/Admin/QuanLyNhanSu/ChiTiet_TTCaNhanValidation.js"></script>
    <script src="~/ValidationAlert/Admin/QuanLyNhanSu/KhoaTaiKhoanValidation.js"></script>
</div>

<table hidden id="exportEmployee" class="table table-hover" style="width:100%">
    <tbody>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Mã Nhân Viên: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.ID_Employee</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Họ và Tên: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Name</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Chức Danh: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Position.Name</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Bộ Phận: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Position.Department.Name</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Lương Hợp Đồng: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Wage.ToString("0,0").Replace(".", ",") đ</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Ngày Vào Làm: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.JoinedDate.ToString("dd/MM/yyyy")</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Quốc Tịch: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Nationality</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">CMND/CCCD: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">'@Model.IdentityCard</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Số Điện Thoại: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.TelephoneMobile</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Địa Chỉ Email: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.WorkEmail</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Sinh Nhật: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Birthday.ToString("dd/MM/yyyy")</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Giới Tính: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Sex</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Địa Chỉ: </h5>
            </td>
            <td class="align-middle border-top-0">
                <span class="mb-0">@Model.Address</span>
            </td>
        </tr>
        <tr>
            <td class="align-middle border-top-0">
                <h5 class="mb-0">Trạng Thái: </h5>
            </td>
            <td class="align-middle border-top-0">
                @if (Model.Lock == true)
                {
                    <span>Đã ngưng làm</span>
                }
                else
                {
                    <span>Hoạt động</span>
                }
            </td>
        </tr>

        @if (Model.Lock == true)
        {
            <tr>
                <td class="align-middle border-top-0">
                    <h5 class="mb-0">Ngày Nghỉ: </h5>
                </td>
                <td class="align-middle border-top-0">
                    <span class="mb-0">@Model.DayOff.Value.ToString("dd/MM/yyyy")</span>
                </td>
            </tr>
        }
    </tbody>
</table>

<script src="~/Content/Admin/assets/js/reviewImage.js"></script>
<script>
    var somethingChanged = false;
    $('#noidungPage').change(function () {
        somethingChanged = true;
    });

    $('#btnLuuThongTin').on('click', function () {
        somethingChanged = false;
    });

    $('#resetData').on('click', function () {
        somethingChanged = false;
        $("#diachiemailcongtyvalidation").hide();
    });


    //Tải xuống thông tin Nhân viên
    $('#taiXuongNhanVien').on('click', function () {
        var today = new Date();
        var dd = String(today.getDate()).padStart(2, '0');
        var mm = String(today.getMonth() + 1).padStart(2, '0');
        var yyyy = today.getFullYear();

        var SweetAlert2Demo = function () {
            var initDemos = function () {
                swal({
                    title: 'Tải xuống thông tin nhân viên?',
                    text: "Bạn có muốn tải xuống thông tin của nhân viên với định dạng file Excel?",
                    type: 'warning',
                    buttons: {
                        cancel: {
                            visible: true,
                            text: ' Hủy Bỏ ',
                            className: 'btn btn-danger'
                        },
                        confirm: {
                            text: 'Xác Nhận',
                            className: 'btn btn-success'
                        }
                    }
                }).then((taixuong) => {
                    if (taixuong) {
                        $("#exportEmployee").table2excel({
                            filename: 'thong-tin-nhan-vien-' + $('#maNhanVienss').val() + '.xls'
                        });
                    }
                });
            };
            return {
                init: function () {
                    initDemos();
                },
            };
        }();

        jQuery(document).ready(function () {
            SweetAlert2Demo.init();
        });

    });

    $(document).ready(function () {

        //table2excel.js
        ; (function ($, window, document, undefined) {
            var pluginName = "table2excel",

                defaults = {
                    exclude: ".noExl",
                    name: "Table2Excel",
                    filename: "table2excel",
                    fileext: ".xls",
                    exclude_img: true,
                    exclude_links: true,
                    exclude_inputs: true
                };

            // The actual plugin constructor
            function Plugin(element, options) {
                this.element = element;
                // jQuery has an extend method which merges the contents of two or
                // more objects, storing the result in the first object. The first object
                // is generally empty as we don't want to alter the default options for
                // future instances of the plugin
                //
                this.settings = $.extend({}, defaults, options);
                this._defaults = defaults;
                this._name = pluginName;
                this.init();
            }

            Plugin.prototype = {
                init: function () {
                    var e = this;

                    var utf8Heading = "<meta http-equiv=\"content-type\" content=\"application/vnd.ms-excel; charset=UTF-8\">";
                    e.template = {
                        head: "<html xmlns:o=\"urn:schemas-microsoft-com:office:office\" xmlns:x=\"urn:schemas-microsoft-com:office:excel\" xmlns=\"http://www.w3.org/TR/REC-html40\">" + utf8Heading + "<head><!--[if gte mso 9]><xml><x:ExcelWorkbook><x:ExcelWorksheets>",
                        sheet: {
                            head: "<x:ExcelWorksheet><x:Name>",
                            tail: "</x:Name><x:WorksheetOptions><x:DisplayGridlines/></x:WorksheetOptions></x:ExcelWorksheet>"
                        },
                        mid: "</x:ExcelWorksheets></x:ExcelWorkbook></xml><![endif]--></head><body>",
                        table: {
                            head: "<table>",
                            tail: "</table>"
                        },
                        foot: "</body></html>"
                    };

                    e.tableRows = [];

                    // get contents of table except for exclude
                    $(e.element).each(function (i, o) {
                        var tempRows = "";
                        $(o).find("tr").not(e.settings.exclude).each(function (i, p) {

                            tempRows += "<tr>";
                            $(p).find("td,th").not(e.settings.exclude).each(function (i, q) { // p did not exist, I corrected

                                var rc = {
                                    rows: $(this).attr("rowspan"),
                                    cols: $(this).attr("colspan"),
                                    flag: $(q).find(e.settings.exclude)
                                };

                                if (rc.flag.length > 0) {
                                    tempRows += "<td> </td>"; // exclude it!!
                                } else {
                                    if (rc.rows & rc.cols) {
                                        tempRows += "<td>" + $(q).html() + "</td>";
                                    } else {
                                        tempRows += "<td";
                                        if (rc.rows > 0) {
                                            tempRows += " rowspan=\'" + rc.rows + "\' ";
                                        }
                                        if (rc.cols > 0) {
                                            tempRows += " colspan=\'" + rc.cols + "\' ";
                                        }
                                        tempRows += "/>" + $(q).html() + "</td>";
                                    }
                                }
                            });

                            tempRows += "</tr>";
                            console.log(tempRows);

                        });
                        // exclude img tags
                        if (e.settings.exclude_img) {
                            tempRows = exclude_img(tempRows);
                        }

                        // exclude link tags
                        if (e.settings.exclude_links) {
                            tempRows = exclude_links(tempRows);
                        }

                        // exclude input tags
                        if (e.settings.exclude_inputs) {
                            tempRows = exclude_inputs(tempRows);
                        }
                        e.tableRows.push(tempRows);
                    });

                    e.tableToExcel(e.tableRows, e.settings.name, e.settings.sheetName);
                },

                tableToExcel: function (table, name, sheetName) {
                    var e = this, fullTemplate = "", i, link, a;

                    e.format = function (s, c) {
                        return s.replace(/{(\w+)}/g, function (m, p) {
                            return c[p];
                        });
                    };

                    sheetName = typeof sheetName === "undefined" ? "Sheet" : sheetName;

                    e.ctx = {
                        worksheet: name || "Worksheet",
                        table: table,
                        sheetName: sheetName
                    };

                    fullTemplate = e.template.head;

                    if ($.isArray(table)) {
                        for (i in table) {
                            //fullTemplate += e.template.sheet.head + "{worksheet" + i + "}" + e.template.sheet.tail;
                            fullTemplate += e.template.sheet.head + sheetName + i + e.template.sheet.tail;
                        }
                    }

                    fullTemplate += e.template.mid;

                    if ($.isArray(table)) {
                        for (i in table) {
                            fullTemplate += e.template.table.head + "{table" + i + "}" + e.template.table.tail;
                        }
                    }

                    fullTemplate += e.template.foot;

                    for (i in table) {
                        e.ctx["table" + i] = table[i];
                    }
                    delete e.ctx.table;

                    var isIE = false || !!document.documentMode; // this works with IE10 and IE11 both :)
                    //if (typeof msie !== "undefined" && msie > 0 || !!navigator.userAgent.match(/Trident.*rv\:11\./))      // this works ONLY with IE 11!!!
                    if (isIE) {
                        if (typeof Blob !== "undefined") {
                            //use blobs if we can
                            fullTemplate = e.format(fullTemplate, e.ctx); // with this, works with IE
                            fullTemplate = [fullTemplate];
                            //convert to array
                            var blob1 = new Blob(fullTemplate, { type: "text/html" });
                            window.navigator.msSaveBlob(blob1, getFileName(e.settings));
                        } else {
                            //otherwise use the iframe and save
                            //requires a blank iframe on page called txtArea1
                            txtArea1.document.open("text/html", "replace");
                            txtArea1.document.write(e.format(fullTemplate, e.ctx));
                            txtArea1.document.close();
                            txtArea1.focus();
                            sa = txtArea1.document.execCommand("SaveAs", true, getFileName(e.settings));
                        }

                    } else {
                        var blob = new Blob([e.format(fullTemplate, e.ctx)], { type: "application/vnd.ms-excel" });
                        window.URL = window.URL || window.webkitURL;
                        link = window.URL.createObjectURL(blob);
                        a = document.createElement("a");
                        a.download = getFileName(e.settings);
                        a.href = link;

                        document.body.appendChild(a);

                        a.click();

                        document.body.removeChild(a);
                    }

                    return true;
                }
            };

            function getFileName(settings) {
                return (settings.filename ? settings.filename : "table2excel");
            }

            // Removes all img tags
            function exclude_img(string) {
                var _patt = /(\s+alt\s*=\s*"([^"]*)"|\s+alt\s*=\s*'([^']*)')/i;
                return string.replace(/<img[^>]*>/gi, function myFunction(x) {
                    var res = _patt.exec(x);
                    if (res !== null && res.length >= 2) {
                        return res[2];
                    } else {
                        return "";
                    }
                });
            }

            // Removes all link tags
            function exclude_links(string) {
                return string.replace(/<a[^>]*>|<\/a>/gi, "");
            }

            // Removes input params
            function exclude_inputs(string) {
                var _patt = /(\s+value\s*=\s*"([^"]*)"|\s+value\s*=\s*'([^']*)')/i;
                return string.replace(/<input[^>]*>|<\/input>/gi, function myFunction(x) {
                    var res = _patt.exec(x);
                    if (res !== null && res.length >= 2) {
                        return res[2];
                    } else {
                        return "";
                    }
                });
            }

            $.fn[pluginName] = function (options) {
                var e = this;
                e.each(function () {
                    if (!$.data(e, "plugin_" + pluginName)) {
                        $.data(e, "plugin_" + pluginName, new Plugin(this, options));
                    }
                });

                // chain jQuery functions
                return e;
            };

        })(jQuery, window, document);

    })


    $(document).on('click', '#donnghiphep', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("donNghiPhep", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
            url: '@Url.Action("donNghiPhep", "QuanLyNhanSu", new {id = Model.ID })',
            type: 'GET',
            dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#danhmucngayphep', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("danhMucNgayPhep", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
            url: '@Url.Action("danhMucNgayPhep", "QuanLyNhanSu", new {id = Model.ID })',
            type: 'GET',
            dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#ttcanhan', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                            url: '@Url.Action("thongTinChiTietPartial", "QuanLyNhanSu", new {id = Model.ID })',
                            type: 'GET',
                            dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });


                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
            url: '@Url.Action("thongTinChiTietPartial", "QuanLyNhanSu", new {id = Model.ID })',
            type: 'GET',
            dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#lhvathanhtoan', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("lienHeVaThanhToanPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("lienHeVaThanhToanPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),

                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });

        }
    });

    $(document).on('click', '#knchuyenmon', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("kyNangChuyenMonPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
            url: '@Url.Action("kyNangChuyenMonPartial", "QuanLyNhanSu", new {id = Model.ID })',
            type: 'GET',
            dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#tdngoaingu', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("trinhDoNgoaiNguPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("trinhDoNgoaiNguPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#ptnhanthan', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("phuThuocNhanThanPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("phuThuocNhanThanPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#tcvaphucap', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("troCapPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("troCapPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });


    $(document).on('click', '#hdvacongviec', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("hopDongPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("hopDongPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#DAThamGia', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("duAnThamGiaPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("duAnThamGiaPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#LSHoatDong', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("lichSuHoatDongPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                    $('#AjaxLoader').fadeOut('slow');
                                }
                                else {
                                    $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }

                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("lichSuHoatDongPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                    $('#AjaxLoader').fadeOut('slow');
                }
                else {
                    $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }

            });
        }
    });

    $(document).on('click', '#bangLuongs', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("bangLuongPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("bangLuongPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    $(document).on('click', '#LBCongViec', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("lichBieuPartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });

                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("lichBieuPartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });

    @*$(document).on('click', '#BCThongKe', function (e) {

        if (somethingChanged == true) {
            var SweetAlert2Demo = function () {
                var initDemos = function () {
                    swal({
                        title: 'Rời khỏi trang?',
                        text: "Những thay đổi vẫn chưa được lưu! Tiếp tục rời khỏi trang?",
                        type: 'warning',
                        buttons: {
                            cancel: {
                                visible: true,
                                text: ' Hủy Bỏ ',
                                className: 'btn btn-danger'
                            },
                            confirm: {
                                text: 'Xác Nhận',
                                className: 'btn btn-success'
                            }
                        }
                    }).then((roikhoitrang) => {
                        if (roikhoitrang) {
                            somethingChanged = false;
                            $('#AjaxLoader').fadeIn('slow');
                            $.ajax({
                                url: '@Url.Action("baoCaoThongKePartial", "QuanLyNhanSu", new {id = Model.ID })',
                                type: 'GET',
                                dataType: 'html'
                            }).done(function (ketqua) {
                                 if (ketqua !== "DANHSACH") {
                                    $('#contentPartial').replaceWith(ketqua);
                                    $.when(
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                                        $.Deferred(function (deferred) {
                                            $(deferred.resolve);
                                        })
                                    ).done(function () {
                                        //place your code here, the scripts are all loaded
                                    });
                                     if ($("#taskSummary").length) {
                                         e = {
                                             series: [{
                                                 name: "Closed",
                                                 type: "column",
                                                 data: [11, 18, 20, 32, 19, 25, 30]
                                             }, {
                                                 name: "New",
                                                 type: "line",
                                                 data: [30, 32, 28, 50, 38, 35, 49]
                                             }],
                                             chart: {
                                                 height: 350,
                                                 type: "line",
                                                 toolbar: {
                                                     show: !1
                                                 }
                                             },
                                             plotOptions: {
                                                 bar: {
                                                     horizontal: !1,
                                                     columnWidth: "40%",
                                                     borderRadius: 5
                                                 }
                                             },
                                             markers: {
                                                 colors: [window.theme.dark],
                                                 fillColor: window.theme.gray200
                                             },
                                             colors: [window.theme.lightPrimary, window.theme.primary],
                                             legend: {
                                                 show: !1
                                             },
                                             stroke: {
                                                 width: [0, 4],
                                                 colors: [window.theme.lightPrimary]
                                             },
                                             dataLabels: {
                                                 enabled: !0,
                                                 enabledOnSeries: [1]
                                             },
                                             labels: ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"],
                                             xaxis: {
                                                 labels: {
                                                     style: {
                                                         fontSize: "13px",
                                                         fontWeight: 400,
                                                         colors: window.theme.gray500,
                                                         fontFamily: "inter"
                                                     }
                                                 }
                                             },
                                             yaxis: {
                                                 labels: {
                                                     style: {
                                                         fontSize: "13px",
                                                         fontWeight: 400,
                                                         colors: window.theme.gray500,
                                                         fontFamily: "inter"
                                                     }
                                                 }
                                             }
                                         };
                                         new ApexCharts(document.querySelector("#taskSummary"), e).render()
                                     }
                                     $('#AjaxLoader').fadeOut('slow');
                                }
                                 else {
                                     $('#AjaxLoader').fadeOut('slow');
                                    window.location.href = $('#actionDanhsach').data('request-url');
                                }
                            });
                        }
                    });
                };
                return {
                    init: function () {
                        initDemos();
                    },
                };
            }();

            jQuery(document).ready(function () {
                SweetAlert2Demo.init();
            });

        } else {
            $('#AjaxLoader').fadeIn('slow');
            $.ajax({
                url: '@Url.Action("baoCaoThongKePartial", "QuanLyNhanSu", new {id = Model.ID })',
                type: 'GET',
                dataType: 'html'
            }).done(function (ketqua) {
                 if (ketqua !== "DANHSACH") {
                    $('#contentPartial').replaceWith(ketqua);
                    $.when(
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/plugin/sweetalert/sweetalert.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/js/theme.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/flatpickr/dist/flatpickr.min.js")"),
                        $.getScript("@Url.Content("~/Content/Admin/assets/libs/apexcharts/dist/apexcharts.min.js")"),
                        $.Deferred(function (deferred) {
                            $(deferred.resolve);
                        })
                    ).done(function () {
                        //place your code here, the scripts are all loaded
                    });
                     if ($("#taskSummary").length) {
                         e = {
                             series: [{
                                 name: "Closed",
                                 type: "column",
                                 data: [11, 18, 20, 32, 19, 25, 30]
                             }, {
                                 name: "New",
                                 type: "line",
                                 data: [30, 32, 28, 50, 38, 35, 49]
                             }],
                             chart: {
                                 height: 350,
                                 type: "line",
                                 toolbar: {
                                     show: !1
                                 }
                             },
                             plotOptions: {
                                 bar: {
                                     horizontal: !1,
                                     columnWidth: "40%",
                                     borderRadius: 5
                                 }
                             },
                             markers: {
                                 colors: [window.theme.dark],
                                 fillColor: window.theme.gray200
                             },
                             colors: [window.theme.lightPrimary, window.theme.primary],
                             legend: {
                                 show: !1
                             },
                             stroke: {
                                 width: [0, 4],
                                 colors: [window.theme.lightPrimary]
                             },
                             dataLabels: {
                                 enabled: !0,
                                 enabledOnSeries: [1]
                             },
                             labels: ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"],
                             xaxis: {
                                 labels: {
                                     style: {
                                         fontSize: "13px",
                                         fontWeight: 400,
                                         colors: window.theme.gray500,
                                         fontFamily: "inter"
                                     }
                                 }
                             },
                             yaxis: {
                                 labels: {
                                     style: {
                                         fontSize: "13px",
                                         fontWeight: 400,
                                         colors: window.theme.gray500,
                                         fontFamily: "inter"
                                     }
                                 }
                             }
                         };
                         new ApexCharts(document.querySelector("#taskSummary"), e).render()
                     }
                     $('#AjaxLoader').fadeOut('slow');
                }
                 else {
                     $('#AjaxLoader').fadeOut('slow');
                    window.location.href = $('#actionDanhsach').data('request-url');
                }
            });
        }
    });*@

</script>
